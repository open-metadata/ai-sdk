target/
dbt_packages/
logs/
.user.yml

# Generated seed data (~208 MB). Deterministic (seed=42) — regenerate with
# `make generate-seeds`. Never committed; only `seeds/_seeds.yml` is tracked.
seeds/raw_*/
