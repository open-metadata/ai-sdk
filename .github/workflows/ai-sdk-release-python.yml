name: Release AI SDK Python

on:
  push:
    tags:
      - 'python-v*'
  workflow_dispatch:
    inputs:
      version:
        required: true
        type: string
        description: 'Version to release (e.g., 0.2.0)'
  workflow_call:
    inputs:
      version:
        required: true
        type: string
        description: 'Version to release (e.g., 0.2.0)'

permissions:
  contents: read
  id-token: write

concurrency:
  group: release-python-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-publish:
    name: Build and Publish to PyPI
    runs-on: ubuntu-latest
    environment: publish
    defaults:
      run:
        working-directory: python

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: python/pyproject.toml

      - name: Install build dependencies
        run: pip install build twine

      - name: Install package with dev dependencies
        run: pip install -e ".[dev]"

      - name: Run tests
        run: pytest --ignore=tests/integration

      - name: Extract version
        id: version
        run: |
          INPUT_VERSION="${{ inputs.version || '' }}"
          if [ -n "$INPUT_VERSION" ]; then
            echo "version=$INPUT_VERSION" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/python-v}" >> $GITHUB_OUTPUT
          fi

      - name: Verify package version matches tag
        run: |
          PACKAGE_VERSION=$(python -c "from importlib.metadata import version; print(version('data-ai-sdk'))")
          TAG_VERSION=${{ steps.version.outputs.version }}
          # Compare only first 3 components (major.minor.patch)
          PKG_BASE=$(echo "$PACKAGE_VERSION" | cut -d. -f1-3)
          TAG_BASE=$(echo "$TAG_VERSION" | cut -d. -f1-3)
          if [ "$PKG_BASE" != "$TAG_BASE" ]; then
            echo "Error: pyproject.toml version ($PACKAGE_VERSION) does not match tag version ($TAG_VERSION)"
            exit 1
          fi

      - name: Check if already published
        id: check
        run: |
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" https://pypi.org/pypi/data-ai-sdk/${{ steps.version.outputs.version }}/json)
          if [ "$HTTP_STATUS" = "200" ]; then
            echo "Version ${{ steps.version.outputs.version }} already published to PyPI. Skipping."
            echo "published=true" >> $GITHUB_OUTPUT
          else
            echo "published=false" >> $GITHUB_OUTPUT
          fi

      - name: Build package
        if: steps.check.outputs.published == 'false'
        run: python -m build

      - name: Check package
        if: steps.check.outputs.published == 'false'
        run: twine check dist/*

      - name: Publish to PyPI
        if: steps.check.outputs.published == 'false'
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: python/dist/
