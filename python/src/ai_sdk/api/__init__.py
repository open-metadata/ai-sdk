"""API namespace classes for the AI SDK.

Each module exposes a small class holding the HTTP clients it needs and
verb methods (list/get/create/delete/etc.). The top-level AISdk client
exposes these as lazy properties.

Import directly from the canonical module path:
    from ai_sdk.api.memories import MemoriesAPI
"""
